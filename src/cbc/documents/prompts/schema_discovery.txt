You are analyzing a vendor document (price book, multiplier sheet, or building plan PDF) to discover its structure.

Sample pages from the document are provided below. Return ONLY valid JSON with these fields:

{
  "anchor_pattern": "regex that marks the start of a new record or section (e.g. line starting with a product code)",
  "price_or_key_value_regex": "regex matching each price line or key-value pair in a section",
  "code_regex": "regex matching product codes, part numbers, or door marks",
  "field_schema": [
    {"name": "code", "type": "string", "description": "part number or mark"},
    {"name": "description", "type": "string"},
    {"name": "price", "type": "number", "description": "list or net price when present"}
  ],
  "spans_multiple_pages": true,
  "notes": "brief notes about layout quirks"
}

Rules:
- anchor_pattern must be a valid Python regex string
- price_or_key_value_regex must count individual priceable rows or key-value pairs
- Prefer patterns that match the actual layout in the samples
- Do not invent data not visible in the samples
