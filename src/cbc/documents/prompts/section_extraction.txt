You are extracting structured records from one section of a vendor or building-plan document.

The document schema is:
{{schema_config}}

Section raw text (pages {{page_range}}):
---
{{raw_text}}
---

Return ONLY valid JSON:
{
  "page_range": [start, end],
  "section_title": "short title for this section",
  "description": "one-sentence summary for search routing",
  "tags": ["category", "vendor", "division"],
  "entities_present": ["verbatim codes and labels found in this section"],
  "extracted_records": [
    { "code": "...", "description": "...", "price": null }
  ],
  "expected_key_value_count": 0,
  "produced_record_count": 0
}

Rules:
- entities_present must list every product code, door mark, or SKU verbatim from the text
- extracted_records must follow field_schema; use null for missing fields
- Do not invent codes or prices not in the text
- expected_key_value_count and produced_record_count will be verified — list every match
